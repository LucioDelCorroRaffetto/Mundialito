app = "mundialito-worker"
primary_region = "gru"

[build]
  dockerfile = "Dockerfile"

[env]
  NODE_ENV = "production"

# Worker has no HTTP service — it's a background process only
[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
