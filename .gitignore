node_modules
dist
dist-ssr
*.local
.env
.env.*
*.env
!.env.example
.DS_Store

# TypeScript build cache
tsconfig.tsbuildinfo
*.tsbuildinfo
.claude/

.vercel

# DB migration dumps — contienen datos de usuarios (emails, hashes). NUNCA commitear.
db-dump/
*.dump.json
packages/api/db-dump/
packages/api/local*.db
packages/api/*.db

# Artefacto local de drizzle-kit (out de drizzle.config.ts). El proyecto migra
# por scripts (src/scripts/migrate*.ts) + el toolkit de MIGRATION.md, no con
# migraciones generadas, así que esta carpeta no se versiona.
packages/api/migrations/
